# 観測地点の設定
location:
  latitude: 35.0153
  longitude: 138.5187
  
# 計算対象の日付
# "auto"  -> 今日の日付を使用
# "2025-01-01" -> 指定した日付を使用
target:
  date: "auto"

# 計算設定
settings:
  check_distance_km: 20   # 何km先の山まで考慮するか
  step_minutes: 2         # 時間を遡る刻み幅（分）